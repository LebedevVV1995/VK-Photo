# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'VK-Photo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for VK-Photo
  
  pod 'Firebase/Auth'
  pod 'Firebase/Core'
  pod 'Firebase/Database'
  pod 'Firebase/Storage'
  pod 'JGProgressHUD'
  pod 'SDWebImage'

  target 'VK-PhotoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'VK-PhotoUITests' do
    # Pods for testing
  end

end
